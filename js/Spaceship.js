class spaceship {
    constructor(){
        constructer(x,y,width,height);
    
    this.body =  bodies.rectangle(x,y,width,height);
    this.height = hright;
    this.width = width;
            
    this.body.add.spaceship;

    }
}
